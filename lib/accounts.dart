import 'package:flutter/material.dart';

class SceenAccounts extends StatelessWidget {
  const SceenAccounts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Accounts")),

    );
  }
}
